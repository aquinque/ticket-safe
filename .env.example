# ──────────────────────────────────────────────────────────────────────────────
# Ticket Safe — environment variables template
# Copy this file to .env and fill in your values.
# NEVER commit .env to git (it is gitignored).
# ──────────────────────────────────────────────────────────────────────────────

# ── Supabase (frontend — safe to expose in browser) ──────────────────────────
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<supabase-anon-key>

# ── Supabase Edge Function secrets (set via Supabase Dashboard → Secrets) ────
# Do NOT put these in .env for the frontend — they are server-side only.
#
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# SITE_URL=https://your-production-domain.com
# TICKET_SIGNING_SECRET=<openssl rand -base64 32>
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are auto-injected by Supabase.

# ── Anthropic API Key (for Claude integration) ──────────────────────────────
# IMPORTANT: the Anthropic key is server-side only. Set it as a Supabase
# Edge Function secret named ANTHROPIC_API_KEY (NOT a VITE_ prefixed env
# var — that would expose it in the browser bundle).
#
# In the Supabase dashboard:
#   Project Settings → Edge Functions → Secrets
#   Name: ANTHROPIC_API_KEY
#   Value: sk-ant-...
#
# Used by edge functions: ai-assistant, ai-event-draft, ai-ticket-score.
VITE_ANTHROPIC_API_KEY=your-anthropic-api-key-here
